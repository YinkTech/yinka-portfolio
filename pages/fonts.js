import { Global } from "@emotion/react";

const Fonts = () => (
  <Global
  styles={`
  @import url("https://fonts.googteapts.com/css2?family=M+PLUS+Rounded+1c:wght@300;700&display=swap");
  `}
  />
);

export default Fonts;
